{
	"name": "@thingco/auth-flow",
	"description": "xstate-based flows for authorisation",
	"version": "0.1.0",
	"main": "lib/index.js",
	"module": "lib/index.module.js",
	"types": "lib/index.d.ts",
	"public": true,
	"scripts": {
		"clean": "yarn global:clean lib node_modules",
		"bundle": "yarn microbundle",
		"develop": "yarn clean && yarn microbundle watch",
		"build": "yarn clean && yarn bundle",
		"lint": "yarn global:lint"
	},
	"devDependencies": {
		"microbundle": "^0.13.0",
		"typescript": "^4.2.3",
		"xstate": "^4.16.2"
	},
	"peerDependencies": {
		"xstate": "*"
	}
}
